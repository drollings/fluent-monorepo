{
  "meta": {
    "module": "src.coral.common.repl",
    "source": "src/coral/common/repl.zig",
    "language": "zig"
  },
  "comment": "Implements a Zig REPL interface handling commands, parsing input and managing stdout/stdin streams.",
  "detail": "# Coral REPL Module Documentation\n\n## Module Purpose and Architecture\n\nThe `repl.zig` module implements an interactive command-line interface (REPL) for the Coral project—a Zig-based build system or target management tool. It provides users with an interactive shell to query, manipulate, and visualize build targets through a series of dot-prefixed commands.\n\n### Architectural Overview\n\n```\n┌─────────────────────────────────────────────────────────────┐\n│                        Repl (This Module)                     │\n├──────────────────────────┬───────────────────────────────────┤\n│  stdin/stdout Wrappers   │  Command Router                   │\n│  (io.ReaderState/        │  ┌─────────────┬────────────────┐ │\n│   io.WriterState)        │  │.handleDot   │.handleBuild    │ │\n│                          │  │Command     │Command         │ │\n└──────────┬───────────────┴────────┬────────┴────────────────┘ │\n           │                       │                           │\n           ▼                       ▼                           ▼\n┌───────────────┐      ┌────────────────┐      ┌─────────────────────┐\n│ BuildContext  │      │ TargetRegistry │      │ JSON Parser         │\n│ (context.zig) │      │ (registry.zig) │      │ (json_parser.zig)   │\n└───────────────┘      └────────────────┘      └─────────────────────┘\n```\n\n## Key Abstractions and Relationships\n\n| Type | Module | Responsibility |\n|------|--------|----------------|\n| `Repl` | This module | Main REPL engine, command routing |\n| `BuildContext` | context.zig | Build system state, target queries |\n| `TargetRegistry` | registry.zig | Target storage and management |\n| `StringInterner` | interner.zig | Efficient string deduplication |\n\nThe `Repl` struct holds a reference to `BuildContext`, which acts as the bridge to external build system functionality. All commands ultimately delegate to methods on `ctx` (e.g., `ctx.listTargets()`, `ctx.showGraph()`).\n\n## Public API and Usage Patterns\n\n### Initialization\n```zig\nconst repl = Repl.init(allocator, ctx);\ndefer repl.deinit();  // No-op currently\ntry repl.run();\n```\n\n### Command Flow\n1. User types command at prompt `coral>`\n2. Input is trimmed of whitespace\n3. If line starts with `.`, routed to `handleDotCommand()`\n4. Otherwise, routed to `handleBuildCommand()` (build-related)\n5. Commands return success/failure; `.quit` breaks the main loop\n\n### Supported Commands\n| Command | Description |\n|---------|-------------|\n| `.help` | Display available commands |\n| `.list` | List all registered targets |\n| `.graph [targets...]` | Visualize target dependencies |\n| `.load <file.json>` | Import targets from JSON file |\n| `.clear` | Reset target registry |\n| `.quit` / `.exit` | Terminate REPL |\n\n## Important Implementation Details\n\n- **Error Handling**: `handleDotCommand()` returns `false` to signal exit; all other commands return `true`\n- **Memory Management**: Uses `std.ArrayListUnmanaged` for command-line argument parsing with proper defer cleanup\n- **I/O Pattern**: Pre-flushes after each prompt and command output for responsive terminal behavior\n- **Tokenization**: Single-space delimiter used throughout for simplicity\n- **JSON Parsing**: Delegates to external parser that takes allocator, registry, and interner references\n\n## Design Patterns Used\n\n1. **Command Router** - Centralizes command handling logic with extensible dispatch\n2. **State Machine** - Main loop uses `continue`/`break` for flow control based on user input\n3. **Builder Pattern** - `init()`/`deinit()` lifecycle management\n4. **Delegation** - Most business logic delegates to `BuildContext` methods",
  "keywords": ["Repl", "Coral", "Zig REPL", "command router", "target registry", "build context", "interactive shell", "stdin/stdout wrappers", "JSON parser delegation", "dependency graph visualization"],
  "members": [
    {
      "type": "fn_decl",
      "name": "init",
      "match_hash": "5fe11cb9d5d51fc4d4a8e0c44494c5535e25532a7998d01b070a2d1a38f7d43c",
      "signature": "fn init(allocator: std.mem.Allocator, ctx: *BuildContext) -> Repl",
      "params": [
        { "name": "allocator", "type": "std.mem.Allocator" },
        { "name": "ctx", "type": "*BuildContext" }
      ],
      "returns": "Repl",
      "comment": "Initializes the Zig environment with an allocator and context, returning a compiled repl instance.",
      "is_pub": true,
      "line": 13
    },
    {
      "type": "fn_decl",
      "name": "deinit",
      "match_hash": "48e322a15afd825a2f74779319d8289a984148490eda117d34b3f91ac99a95cd",
      "signature": "fn deinit(_: *Repl) -> void",
      "params": [
        { "name": "_", "type": "*Repl" }
      ],
      "returns": "void",
      "comment": "Releases resources and cleans up the Zig environment when the repl ends.",
      "is_pub": true,
      "line": 20
    },
    {
      "type": "fn_decl",
      "name": "run",
      "match_hash": "9ab4f9974fd768f246034d3960f63ffa568a8d635d3a54f3a0103da074be9540",
      "signature": "fn run(self: *Repl) -> void",
      "params": [
        { "name": "self", "type": "*Repl" }
      ],
      "returns": "void",
      "comment": "Executes the Zig compiler pass, processing the provided source code and returning compilation results.",
      "is_pub": true,
      "line": 22
    },
    {
      "type": "fn_private",
      "name": "handleDotCommand",
      "match_hash": "7090e29e73a37982ea5a382b05648478758fecc06f231927a8037c9a7e755d31",
      "signature": "fn handleDotCommand(self: *Repl, input: []const u8, writer: *std.Io.Writer) -> bool",
      "params": [
        { "name": "self", "type": "*Repl" },
        { "name": "input", "type": "[]const u8" },
        { "name": "writer", "type": "*std.Io.Writer" }
      ],
      "returns": "bool",
      "comment": "Processes a Zig command string, validates input, and returns a boolean status.",
      "is_pub": false,
      "line": 61
    },
    {
      "type": "fn_private",
      "name": "handleBuildCommand",
      "match_hash": "1074af0e306cd4d63f59a66a245a71f785ecb428efdeff90bd779584b9999c5f",
      "signature": "fn handleBuildCommand(self: *Repl, input: []const u8, writer: *std.Io.Writer) -> void",
      "params": [
        { "name": "self", "type": "*Repl" },
        { "name": "input", "type": "[]const u8" },
        { "name": "writer", "type": "*std.Io.Writer" }
      ],
      "returns": "void",
      "comment": "Processes a build command input and writes the result to the writer.",
      "is_pub": false,
      "line": 126
    }
  ]
}
